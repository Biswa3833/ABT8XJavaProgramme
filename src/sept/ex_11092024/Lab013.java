package sept.ex_11092024;

public class Lab013 {
    public static void main(String[] args) {
        // to create variable there is some rules
        // we need to follow the rules to create variable_name
        //variable_name (identifier) should be start with - Alphabets , Digits and two special symbol '$' and "_"
        // first character of a variable_name should be Alphabet or $ or _ .
        // Keywords or Reserve words can not be use as variable_name
        // valid Identifiers = int , Hello , getClassName , StudentEmail, TOTAL_FEE etc.
        boolean $ = true;
        boolean _ = true;
        boolean pramod_is_very_good_person = true;
        // boolean 123pramod = true; - wrong
        // number can not be a stater of a variable or identifier
        boolean pramod123 = true;
        boolean _123 = true;
        System.out.println(_123);
        // exit code 0 is success and no error
        boolean PRAMOD = true;
        // you can write the variable_name with capital letter
        // boolean pramod dutta = true; - wrong
        // space is not allowed in variable_name
        // you can use '$' or '_' in between those space
        boolean pramod$dutta = true;
        boolean pramod_dutta = true;


       // boolean 123 = true; - wrong



    }
}
